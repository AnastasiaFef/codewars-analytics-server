const roles = {
  admin: [
    // USER
    'user.get.all',

    // GROUP
    'group.create',
    'group.update',
    'group.delete',
  ],
  student: [
    // USER
    'user.get.all',
  ],
};

export default roles;
